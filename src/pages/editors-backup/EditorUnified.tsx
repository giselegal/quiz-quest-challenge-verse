import React from 'react';

// Placeholder page kept for historical reasons. The real editor lives elsewhere.
const EditorUnifiedPage: React.FC = () => {
  return (
    <div className="p-8">
      <h2 className="text-lg font-medium">EditorUnified (placeholder)</h2>
      <p className="text-sm text-gray-600">Este arquivo é apenas um placeholder para evitar erros de build.</p>
    </div>
  );
};

export default EditorUnifiedPage;
